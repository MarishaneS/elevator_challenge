import { AmountAllowedDirective } from './amount-allowed.directive';

describe('AmountAllowedDirective', () => {
  it('should create an instance', () => {
    const directive = new AmountAllowedDirective();
    expect(directive).toBeTruthy();
  });
});
