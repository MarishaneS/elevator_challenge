import { FloorNotExistDirective } from './floor-not-exist.directive';

describe('FloorNotExistDirective', () => {
  it('should create an instance', () => {
    const directive = new FloorNotExistDirective();
    expect(directive).toBeTruthy();
  });
});
